#include "main.h"
/**
 * add - Adds two integers and prints the result
 * @prime: first number
 * @final: second number
 * Return: prime + final
 */
int add(int prime, int final)
{
	return (prime + final);
}
